Check all the syntaxes, especially finally lines
Add code to install the prerequisites like 'llama_cpp' and other if not exist, install this without any issues and cross check the intsallation.
Data required to process is present in "Data" folder and "listings_sample.csv" is the file. Write the full application in the Python.
(Note: donnot use Fiass use alternative, donnot use sentence_transformers)
1. Data task: Clean and explore a small real-estate dataset (listings with price, beds, baths, city, remarks).
2. Predictive task: Train a simple model (e.g., predict price_per_sqft or a price band).
3. LLM integration: Build one of:
• RAG mini-Q&A over listing remarks (embedding + top-k retrieval), or

• Auto-summary generator for a listing (2–3 sentences) using facts from the row.

Create a simple user friendly UI app for the above steps and show that which model is used for what and the results. And also show the graphical comparisions of the cities, prices etc of the result obtained. 
(Use local/open-source models. No paid external API keys required.)

The RAG Q&A over remarks tab may fail as it is not installed, handle the installations or use someother model for this feature.
Handle the errors, Listing Q&A (RAG) is not giving the correct answers for specific questions.

Polish the UI improve it.

Give error free functional code.

For the QnA option, include the summary generation similar to below details:
id, Address, Price, Beds, Baths, Remarks, Summary 
